# LSeg weights manifest

说明：不把 .pth 提交到 git，只记录本机路径与用途。

## Default (used in our runs)
- /home/qile/ovss_runs/v6_bnfreeze_stage1/best_pareto.pth
  - produced by: finetune_lseg_loveda_head_v6.py (v6 stage1)
  - recommended config: prompt_set=v2, agg=max, no_bias

